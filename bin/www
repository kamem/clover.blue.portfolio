#!/usr/bin/env node
var app = require('../root/app.js').default;
var settings = require('../root/settings').default;

var server = app.listen(settings.port, () => {
  console.log('Express server listening on port ' + server.address().port);
});